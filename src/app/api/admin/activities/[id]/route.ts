import {NextRequest,NextResponse} from 'next/server';import {adminSupabase} from '@/lib/supabase';
export async function PATCH(req:NextRequest,{params}:{params:Promise<{id:string}>}){if(req.headers.get('x-activat-admin-secret')!==process.env.ACTIVAT_ADMIN_SECRET)return NextResponse.json({error:'No autoritzat.'},{status:401});const {id}=await params;const b=await req.json();const patch:any={};if(b.capacity_override!==undefined)patch.capacity_override=Math.max(0,Number(b.capacity_override));if(b.vacancies_open!==undefined)patch.vacancies_open=Boolean(b.vacancies_open);try{const sb=adminSupabase();const {data,error}=await sb.from('activities').update(patch).eq('id',id).select('*').single();if(error)throw error;return NextResponse.json(data)}catch{return NextResponse.json({error:'No s’ha pogut actualitzar l’activitat.'},{status:500})}}
